name: mi_aplicacion_gotica
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  audioplayers: ^5.2.1
  http: ^0.13.6  # 👈 Esta es la nueva dependencia que añadimos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fondo_gotico.jpg
    - assets/audio/
  fonts:
    - family: Unifraktur
      fonts:
        - asset: assets/fonts/UnifrakturCook-Bold.ttf
